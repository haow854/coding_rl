# AutoDL image: prefer a PyTorch/CUDA image with torch already installed.
# Do not reinstall torch unless the image is broken.

# Qwen3.5 support is recent; keep the HF stack current on the GPU box.
transformers>=5.0
trl>=1.7
peft>=0.17
datasets>=2.18
accelerate>=1.0
evalplus>=0.3.1

# Optional fast rollout/eval. Install only if it matches the image's torch/CUDA:
#   pip install "vllm>=0.10"
