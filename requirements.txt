# Single RunPod/GPU environment file for data, SFT/GRPO, vLLM eval, and official
# LiveCodeBench. Prefer a PyTorch/CUDA RunPod image with torch already installed;
# install this without `-U` so pip does not casually replace the image's CUDA
# matched torch build.

# Core data / utilities
torch
numpy
pydantic>=2.6
pyyaml
huggingface_hub
datasets>=2.20
matplotlib

# HF training stack
transformers>=5.0
trl>=1.7
peft>=0.17
accelerate>=1.0

# vLLM generation / eval
vllm>=0.10
flashinfer-python
nvidia-cuda-nvrtc

# Evaluation harnesses
evalplus>=0.3.1
git+https://github.com/LiveCodeBench/LiveCodeBench.git
