# Single RunPod/GPU environment file for data, SFT/GRPO, vLLM eval, and official
# LiveCodeBench. Prefer a PyTorch/CUDA RunPod image with torch already installed;
# install this without `-U` so pip does not casually replace the image's CUDA
# matched torch build.

# Core data / utilities
torch
numpy
pydantic>=2.6
pyyaml
huggingface_hub
datasets>=2.20
matplotlib

# HF training stack
transformers>=5.0
trl>=1.7
peft>=0.17
accelerate>=1.0

# vLLM generation / eval
vllm>=0.10
flashinfer-python
nvidia-cuda-nvrtc

# Evaluation harnesses. LiveCodeBench itself is used from a source checkout at
# ./LiveCodeBench because upstream packaging currently omits lcb_runner
# subpackages from the pip install.
evalplus>=0.3.1
pebble>=5.1.0
annotated-types>=0.7.0
openai>=1.59.6
anthropic>=0.42.0
cohere>=5.13.6
google-genai>=0.6.0
mistralai==0.4.2
together>=0.21.0
